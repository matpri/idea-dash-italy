import pandas as pd


def check_folder(folder):
    # check if folder contains files that are needed for processing
    return True

def process(folders:dict):
    # process the folders and return dataframe, dict={scenario_name: folder}
    return pd.DataFrame()

